# NOTICE

本 Package 不分发任何模型权重。它只**编排**别的 Asset Package 声明的资产：
读目录、读上游元数据、读本机账本、触发 Framework 的安装/取用/校验/删除。

模型的许可归各自的 Asset Package 与其上游所有：
`github.termux-os.asset.sensevoice`、`github.termux-os.asset.fireredvad`、
`github.termux-os.asset.campplus`、`github.termux-os.asset.qwen3asr` 等。
本包的代码以 Apache-2.0 发布（见 `LICENSE`）。
